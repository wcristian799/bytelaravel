<?php

beforeEach(function () {
    $this->user = createAdmin();
});

test('example', function () {
    expect(true)->toBeTrue();
})->skip('This is an example test, remove this line to run this test');
